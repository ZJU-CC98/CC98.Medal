@using System.Text.Json
@using JetBrains.Annotations
@model IEnumerable<MedalCategory>
@{

	var topItems
		= from i in Model
		  where i.ParentId == null
		  select i;
}

<partial name="MedalCategoryTreeTemplate" model="topItems" />
